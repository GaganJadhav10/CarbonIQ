# Runtime dependencies. Render installs exactly this file.
# Dev/test tooling lives in requirements-dev.txt so the deployed image stays lean.

fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database. psycopg2 (sync) rather than asyncpg -- see README "Trade-offs":
# asyncpg against Supabase's PgBouncer pooler requires prepared-statement
# workarounds that sync drivers simply do not need.
SQLAlchemy==2.0.36
GeoAlchemy2==0.16.0
psycopg2-binary==2.9.10

# Config and validation
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# Auth. PyJWT over python-jose (maintained); bcrypt directly over passlib
# (passlib 1.7.4 warns noisily against modern bcrypt releases).
PyJWT==2.10.1
bcrypt==4.2.1
