# Development and CI tooling. Not installed on Render.
-r requirements.txt

pytest==8.3.4
httpx==0.28.1

# Used by db/apply_schema.py to read backend/.env outside the FastAPI app.
python-dotenv==1.0.1
ruff==0.8.4
black==24.10.0
